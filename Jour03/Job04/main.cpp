#include <iostream>
#include <string>

int main()
{
    std::string str = "vive la plateforme !";
    int len = str.length();

    std::cout << "La taille de : \"" << str << "\" est " << len << std::endl;
}